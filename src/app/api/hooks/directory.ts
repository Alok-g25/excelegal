const profilePath = `./public/profiles/`
const categoryPath = `./public/categories/`
const uploadPath = `./public/uploads/`
const bannerPath = `./public/banners/`
const resumePath="./public/resume-cv/"
export const useDirectory = () => {
    return { profilePath, categoryPath, uploadPath, bannerPath,resumePath};
};